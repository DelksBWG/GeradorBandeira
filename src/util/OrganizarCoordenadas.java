package util;

import entity.Coordenadas;
import entity.Linha;

public class OrganizarCoordenadas {
	public Coordenadas Interseccao(Linha linha1, Linha linha2) {
		Coordenadas coorFinal = null;
			
		return coorFinal;
	}
	
	private float valorYComXZero(Linha linha) {
		
		return linha.getInicio().getY();
	}
}
